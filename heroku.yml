setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE
build:
  docker:
    web: heroku.Dockerfile
#release:
#  command:
#    - make wait-for-db release
#  image: web
run:
  web: make run-prod