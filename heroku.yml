setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE
  config:
    MODE_DEBUG: 0
    PYTHONPATH: src
build:
  docker:
    web: heroku.Dockerfile
release:
  command:
    - make wait-for-db release
  image: web
run:
  web: make run-prod
