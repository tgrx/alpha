setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE
  config:
build:
  docker:
    web: Dockerfile
release:
  command:
    - make wait-for-db release
  image: web
run:
  web: make run-prod